# frozen_string_literal: true

RSpec.describe CatFacts do
  it "has a version number" do
    expect(CatFacts::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
